<% num_followers_past_days = Follow.followable_user(@user.id).where("created_at > ?", 24.hours.ago).size %>

<% if num_followers_past_days.positive? %>
  <h3 style="font-weight:600;font-size:0.95em;color:#3ac1b4">You have <%= num_followers_past_days %> new <%= community_name %> followers in the past day!</h3>
<% end %>

<h2 style="font-size:32px;">
  <a href="<%= user_url(@follower) %>"><%= @follower.name %></a>seni takip etti 🤗</h2>
<h3 style="font-weight:600">Toplam <%= pluralize(@user.followers_count, "total follower") %> takipçiniz var. 
  <a href="<%= app_url("/dashboard/user_followers") %>">right here</a> tümünü görün.</h3>
<p>
  Birisi sizi 
  <a href="<%= app_url %>"><%= community_name %></a>'de takip ettiğinde, gönderileriniz anasayfalarında ve bildirimlerinde öncelikli görülecektir. Sanırım popülerliğin avantajları.
</p>
